# Contract for "Button" — generated by `figma-cli rules gen`.
# Reviewed once by a human, enforced from then on by `figma-cli check`.
# Edit freely: this file is the source of truth, not the Figma file.
component: Button
page: Page 1
require:
  exists: true
  variants: 3
  exhaustive: true
  tokens:
    fills: bound
    scope: all-variants
  geometry:
    State=Default:
      height: 38
      tolerance: 2
axes:
  State:
    - Default
    - Disabled
    - Hover
